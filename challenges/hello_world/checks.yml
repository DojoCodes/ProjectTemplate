# Unique id of the check
hello_world:
  # Name of the check as displayed on the website if visible
  name: Hello, world!
  # Standard input for this check (string).
  # If input on multiple rows : separate each entry by an empty row
  stdin: null
  # Parameters (or argv) for this check (array)
  parameters: null
  # Expected output (string)
  expected_stdout: Hello, world!
  # Whether the check is visible for the user or not (sample check)
  visible: true
